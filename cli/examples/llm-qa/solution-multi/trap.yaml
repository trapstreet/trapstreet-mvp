tasks:
  test:
    cmd: sh -c 'set -a && . .env && set +a && uv run solution.py'
    traptask:
      local: ../task
    inputs:
      files:
        - question.txt
    metadata:
      providers: [openai, claude-code]
      models: [gpt-4o-mini, claude-code]
