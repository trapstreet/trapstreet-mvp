tasks:
  test:
    cmd: sh -c 'set -a && . .env && set +a && uv run solution.py'
    traptask:
      local: ../task
    inputs:
      files:
        - question.txt
    metadata:
      provider: mistral
      model: mistral-small-latest
